<!-- Mobile Toggle Button -->
<button id="mobileToggleBtn" class="mobile-toggle d-lg-none position-fixed top-0 start-0 m-3 btn btn-primary rounded-circle p-3 shadow-lg">
    <i class="fas fa-bars fa-lg"></i>
</button>

<!-- Sidebar Overlay -->
<div id="sidebarOverlay" class="sidebar-overlay"></div>

<!-- Loading Overlay -->
<div class="loading-overlay" id="loadingOverlay">
    <div class="loading-spinner">
        <div class="spinner-border text-primary" role="status"></div>
        <h4>Đang xử lý...</h4>
        <p>Vui lòng chờ trong giây lát</p>
    </div>
</div>

<%- include('sidebar') %>

<li class="nav-item">
    <a class="nav-link <%= currentPage === 'charts' ? 'active' : '' %>" href="/charts">
        <i class="fas fa-chart-pie"></i> Đồ Thị Đánh Giá
    </a>
</li>